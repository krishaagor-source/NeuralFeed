from summarizer import generate_summary

article_text = "OpenAI introduced a new AI model with improved reasoning, faster coding capabilities, and reduced latency compared to earlier versions."
summary = generate_summary(article_text)
print(summary)